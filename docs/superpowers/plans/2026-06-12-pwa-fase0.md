# PWA Fase 0 (app offline + instalável) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o curso estático uma PWA instalável e offline (manifest + ícones + service worker com precache), com deploy no GitHub Pages via Actions.

**Architecture:** Arquivos novos na raiz de `site/` (manifest, sw, precache, ícones) + injeção de metadados PWA no `<head>` de todas as páginas via script idempotente depth-aware (sobrevive ao subpath do Pages). Scripts de geração (ícones via Playwright, precache via fs) rodam no deploy/local, não no runtime. ZERO mudança no motor/conteúdo.

**Tech Stack:** HTML/CSS/JS estático, Service Worker API, `node --test`, Playwright (já devDep, reusa Chromium do smoke), GitHub Actions Pages.

**Spec:** `docs/superpowers/specs/2026-06-12-pwa-fase0-design.md`.

**Convenções herdadas:**
- Web em `site/`. Testes em `test/`. **Baseline: 148 testes + `npm run smoke` (17 módulos) verde.**
- Repo: `hhthunderbird/ShaderWorkshop` → Pages em `https://hhthunderbird.github.io/ShaderWorkshop/` (SUBPATH `/ShaderWorkshop/`).
- **Regra dura:** manifest/SW/ícones/start_url RELATIVOS e depth-aware (`''` raiz, `'../'` em `modulos/` e `professor/`). Nunca absoluto `/sw.js`.
- Páginas a tocar (20): `site/index.html`, `site/glossario.html`, `site/professor/index.html`, `site/modulos/00..16` (17).

---

## Task 1: Teste de PWA (falha primeiro)

**Files:**
- Test: `test/pwa.test.js`

- [ ] **Step 1: Escrever o teste**

Criar `test/pwa.test.js`:

```javascript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, existsSync } from 'node:fs';

const PAGES = [
  'site/index.html', 'site/glossario.html', 'site/professor/index.html',
  ...['00-comecando','01-shaders-e-gpu','02-pixel-e-cor','03-matematica-vira-imagem',
      '04-formas-e-padroes','05-dando-vida-animacao','06-paralelismo','07-vertices-e-pipeline',
      '08-vetores-e-coordenadas','09-texturas-e-uv','10-normais-e-luz','11-hardware-fixo',
      '12-luz-especular','13-alem-de-pixels','14-otimizacao','15-placa-de-video','16-transparencia']
    .map((m) => `site/modulos/${m}.html`),
];

test('manifest: JSON valido com campos PWA obrigatorios', () => {
  const m = JSON.parse(readFileSync('site/manifest.webmanifest', 'utf8'));
  assert.equal(m.display, 'standalone');
  assert.ok(m.name && m.short_name, 'falta name/short_name');
  assert.equal(m.start_url, '.');
  assert.ok(m.theme_color && m.background_color, 'falta theme/background color');
  const sizes = m.icons.map((i) => i.sizes);
  assert.ok(sizes.includes('192x192') && sizes.includes('512x512'), 'faltam icones 192/512');
  assert.ok(m.icons.some((i) => i.purpose === 'maskable'), 'falta icone maskable');
});

test('service worker existe e usa o precache', () => {
  const sw = readFileSync('site/sw.js', 'utf8');
  assert.ok(sw.includes('precache.json'), 'sw nao referencia precache.json');
  assert.ok(/addAll/.test(sw), 'sw nao faz precache (addAll)');
  assert.ok(/caches\.open/.test(sw), 'sw nao abre cache');
});

test('precache.json: array com index e ao menos um modulo', () => {
  const list = JSON.parse(readFileSync('site/precache.json', 'utf8'));
  assert.ok(Array.isArray(list) && list.length > 0, 'precache vazio');
  assert.ok(list.includes('index.html'), 'precache sem index.html');
  assert.ok(list.some((p) => p.startsWith('modulos/')), 'precache sem modulos');
  assert.ok(!list.includes('precache.json') && !list.includes('sw.js'), 'precache nao deve listar a si mesmo nem o sw');
});

test('todas as paginas injetam manifest + registram o service worker', () => {
  for (const p of PAGES) {
    const html = readFileSync(p, 'utf8');
    assert.ok(/rel="manifest"/.test(html), `${p}: falta rel=manifest`);
    assert.ok(/serviceWorker\.register/.test(html), `${p}: falta registro do SW`);
    assert.ok(/name="theme-color"/.test(html), `${p}: falta meta theme-color`);
  }
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npm test`
Expected: FAIL — `manifest.webmanifest`/`sw.js`/`precache.json` não existem; páginas sem injeção.

---

## Task 2: manifest.webmanifest

**Files:**
- Create: `site/manifest.webmanifest`

- [ ] **Step 1: Criar o manifest**

Criar `site/manifest.webmanifest`:

```json
{
  "name": "Curso de Shaders, GPU e Pipeline Gráfica",
  "short_name": "Shaders",
  "description": "Curso interativo de shaders, GPU e pipeline gráfica para o ensino médio.",
  "start_url": ".",
  "scope": ".",
  "display": "standalone",
  "orientation": "any",
  "theme_color": "#d6336c",
  "background_color": "#fffdf7",
  "icons": [
    { "src": "assets/img/app-icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "assets/img/app-icon-512.png", "sizes": "512x512", "type": "image/png" },
    { "src": "assets/img/app-icon-maskable.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}
```

- [ ] **Step 2: Conferir JSON válido**

Run: `node -e "JSON.parse(require('fs').readFileSync('site/manifest.webmanifest','utf8')); console.log('ok')"`
Expected: `ok`.

---

## Task 3: Ícone do app (SVG + geração dos PNGs via Playwright)

**Files:**
- Create: `scripts/gen-pwa-icons.mjs`
- Create (gerados): `site/assets/img/app-icon-192.png`, `app-icon-512.png`, `app-icon-maskable.png`, `apple-touch-icon.png`

- [ ] **Step 1: Criar o gerador de ícones**

Criar `scripts/gen-pwa-icons.mjs` (renderiza um SVG inline em N×N e tira screenshot PNG; reusa o Chromium do smoke):

```javascript
// Gera os PNGs do ícone do app a partir de um SVG inline, via Playwright.
// Rodar: node scripts/gen-pwa-icons.mjs  (precisa do chromium do Playwright)
import { chromium } from 'playwright';
import { writeFile } from 'node:fs/promises';
import { join } from 'node:path';

const OUT = join(process.cwd(), 'site', 'assets', 'img');

// safe = fração central usada pelo motivo (maskable usa menos p/ caber na safe zone).
function svg(safe) {
  const pad = (1 - safe) / 2 * 512;
  const a = pad, b = 512 - pad, mid = 256;
  return `<svg xmlns="http://www.w3.org/2000/svg" width="512" height="512" viewBox="0 0 512 512">
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#1c7ed6"/>
      <stop offset="1" stop-color="#e8590c"/>
    </linearGradient>
  </defs>
  <rect width="512" height="512" rx="96" fill="url(#g)"/>
  <path d="M ${a} ${mid} C ${a + (b - a) * 0.25} ${mid - (b - a) * 0.28}, ${a + (b - a) * 0.45} ${mid - (b - a) * 0.28}, ${mid} ${mid}
           S ${b - (b - a) * 0.05} ${mid + (b - a) * 0.28}, ${b} ${mid}"
        fill="none" stroke="#ffffff" stroke-width="34" stroke-linecap="round" opacity="0.95"/>
</svg>`;
}

async function shoot(page, svgStr, size, file) {
  await page.setViewportSize({ width: size, height: size });
  await page.setContent(
    `<style>html,body{margin:0;padding:0}svg{display:block;width:${size}px;height:${size}px}</style>${svgStr}`,
    { waitUntil: 'networkidle' }
  );
  const buf = await page.screenshot({ omitBackground: false });
  await writeFile(join(OUT, file), buf);
  console.log('  ' + file + ' (' + size + 'px)');
}

const browser = await chromium.launch({ headless: true });
const page = await browser.newPage();
console.log('Gerando ícones do app:');
await shoot(page, svg(0.86), 192, 'app-icon-192.png');
await shoot(page, svg(0.86), 512, 'app-icon-512.png');
await shoot(page, svg(0.62), 512, 'app-icon-maskable.png'); // motivo menor (safe zone)
await shoot(page, svg(0.86), 180, 'apple-touch-icon.png');
await browser.close();
console.log('Ícones gerados em site/assets/img/.');
```

- [ ] **Step 2: Gerar os PNGs**

Run: `node scripts/gen-pwa-icons.mjs`
Expected: imprime os 4 arquivos; `ls site/assets/img/app-icon-*.png site/assets/img/apple-touch-icon.png` mostra os 4 PNGs.

---

## Task 4: precache.json (gerador + saída)

**Files:**
- Create: `scripts/gen-precache.mjs`
- Create (gerado): `site/precache.json`

- [ ] **Step 1: Criar o gerador**

Criar `scripts/gen-precache.mjs`:

```javascript
// Varre site/ e gera site/precache.json (lista de assets p/ o service worker).
// Exclui o proprio precache.json e o sw.js. So usa node built-ins (roda em CI sem npm install).
// Rodar: node scripts/gen-precache.mjs
import { readdir, writeFile, stat } from 'node:fs/promises';
import { join, relative, sep } from 'node:path';

const SITE = join(process.cwd(), 'site');
const SKIP = new Set(['precache.json', 'sw.js']);

async function walk(dir) {
  const out = [];
  for (const name of await readdir(dir)) {
    const full = join(dir, name);
    const st = await stat(full);
    if (st.isDirectory()) out.push(...await walk(full));
    else {
      const rel = relative(SITE, full).split(sep).join('/');
      if (!SKIP.has(rel)) out.push(rel);
    }
  }
  return out;
}

const list = (await walk(SITE)).sort();
await writeFile(join(SITE, 'precache.json'), JSON.stringify(list, null, 0) + '\n');
console.log(`precache.json: ${list.length} arquivos.`);
```

- [ ] **Step 2: Gerar**

Run: `node scripts/gen-precache.mjs`
Expected: imprime a contagem; `site/precache.json` existe, é um array JSON, contém `index.html` e `modulos/...`, NÃO contém `precache.json`/`sw.js`.

---

## Task 5: Service worker

**Files:**
- Create: `site/sw.js`

- [ ] **Step 1: Criar o SW**

Criar `site/sw.js`:

```javascript
// Service worker do curso: precache de todos os assets (offline) + cache-first.
// Bump CACHE quando publicar assets novos.
const CACHE = 'shaders-v1';

self.addEventListener('install', (e) => {
  e.waitUntil((async () => {
    const cache = await caches.open(CACHE);
    const list = await (await fetch('precache.json', { cache: 'no-cache' })).json();
    // inclui o proprio scope ('./') p/ a navegacao raiz funcionar offline
    await cache.addAll(['./', ...list]);
    self.skipWaiting();
  })());
});

self.addEventListener('activate', (e) => {
  e.waitUntil((async () => {
    for (const k of await caches.keys()) if (k !== CACHE) await caches.delete(k);
    await self.clients.claim();
  })());
});

self.addEventListener('fetch', (e) => {
  if (e.request.method !== 'GET') return;
  e.respondWith((async () => {
    const cached = await caches.match(e.request);
    if (cached) return cached;
    try {
      return await fetch(e.request);
    } catch {
      // navegacao offline sem cache -> cai na index cacheada
      if (e.request.mode === 'navigate') {
        const idx = await caches.match('./');
        if (idx) return idx;
      }
      throw new Error('offline e fora do cache');
    }
  })());
});
```

- [ ] **Step 2: Conferir sintaxe**

Run: `node -c site/sw.js`
Expected: sem erro.

---

## Task 6: Injeção do `<head>` em todas as páginas

**Files:**
- Create: `scripts/inject-pwa-head.mjs`
- Modify (via script): as 20 páginas em `site/`

- [ ] **Step 1: Criar o injetor idempotente**

Criar `scripts/inject-pwa-head.mjs`:

```javascript
// Injeta metadados PWA no <head> de todas as paginas (idempotente, depth-aware).
// Prefixo relativo: '' na raiz de site/, '../' em subpastas (modulos/, professor/).
// Rodar: node scripts/inject-pwa-head.mjs
import { readFile, writeFile, readdir } from 'node:fs/promises';
import { join } from 'node:path';

const SITE = join(process.cwd(), 'site');
const MARK = '<!-- pwa-head -->';

function block(prefix) {
  return `${MARK}
  <link rel="manifest" href="${prefix}manifest.webmanifest">
  <meta name="theme-color" content="#d6336c">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
  <link rel="apple-touch-icon" href="${prefix}assets/img/apple-touch-icon.png">
  <script>if('serviceWorker' in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('${prefix}sw.js'));}</script>`;
}

async function inject(file, prefix) {
  let html = await readFile(file, 'utf8');
  if (html.includes(MARK)) return false; // idempotente
  html = html.replace('</head>', block(prefix) + '\n</head>');
  await writeFile(file, html);
  return true;
}

const pages = [
  ['index.html', ''],
  ['glossario.html', ''],
  ['professor/index.html', '../'],
  ...(await readdir(join(SITE, 'modulos'))).filter((f) => f.endsWith('.html')).map((f) => [`modulos/${f}`, '../']),
];

let n = 0;
for (const [rel, prefix] of pages) {
  if (await inject(join(SITE, rel), prefix)) { n++; console.log('  injetado: ' + rel); }
}
console.log(`PWA head injetado em ${n} pagina(s).`);
```

- [ ] **Step 2: Rodar a injeção**

Run: `node scripts/inject-pwa-head.mjs`
Expected: imprime "injetado" para as 20 páginas (ou menos se alguma já tiver). Rodar de novo → 0 injetados (idempotente).

- [ ] **Step 3: Conferir uma página raiz e uma de subpasta**

Run: `grep -c "pwa-head" site/index.html site/modulos/01-shaders-e-gpu.html`
Expected: 1 em cada. Conferir que o módulo usa `../manifest.webmanifest` e a raiz usa `manifest.webmanifest`.

---

## Task 7: Verde + smoke + prova de offline

**Files:**
- Create (temporário): `scripts/verify-pwa-offline.mjs` (deletado no fim)

- [ ] **Step 1: Rodar os testes**

Run: `npm test`
Expected: PASS — os 4 testes de PWA verdes (baseline 148 → 152).

- [ ] **Step 2: Smoke (conteúdo intacto)**

Run: `npm run smoke`
Expected: verde, 17 módulos.

- [ ] **Step 3: Prova de offline via Playwright**

Criar `scripts/verify-pwa-offline.mjs`:

```javascript
// Prova throwaway: registra o SW, fica offline, e confirma que um modulo abre do cache.
import { chromium } from 'playwright';
import { createServer } from 'node:http';
import { readFile } from 'node:fs/promises';
import { join, extname } from 'node:path';

const SITE = join(process.cwd(), 'site');
const MIME = { '.html':'text/html; charset=utf-8', '.js':'text/javascript; charset=utf-8', '.css':'text/css; charset=utf-8', '.json':'application/json', '.svg':'image/svg+xml', '.png':'image/png', '.webmanifest':'application/manifest+json' };
const server = createServer(async (req, res) => {
  try {
    const p = decodeURIComponent(req.url.split('?')[0]);
    const fp = join(SITE, p === '/' ? 'index.html' : p);
    const body = await readFile(fp);
    res.writeHead(200, { 'Content-Type': MIME[extname(fp)] || 'application/octet-stream' });
    res.end(body);
  } catch { res.writeHead(404); res.end('nf'); }
});
await new Promise((r) => server.listen(0, r));
const base = `http://localhost:${server.address().port}`;

const browser = await chromium.launch({ headless: true });
const ctx = await browser.newContext();
const page = await ctx.newPage();
await page.goto(base + '/index.html', { waitUntil: 'networkidle' });
await page.evaluate(() => navigator.serviceWorker.ready);
await page.waitForTimeout(500); // deixa o precache assentar
await ctx.setOffline(true);
let ok = false;
try {
  await page.goto(base + '/modulos/01-shaders-e-gpu.html', { waitUntil: 'domcontentloaded', timeout: 8000 });
  ok = await page.evaluate(() => document.title.length > 0 && !!document.querySelector('h1'));
} catch (e) { ok = false; }
console.log('offline: modulo abriu do cache?', ok);
await browser.close();
await new Promise((r) => server.close(r));
process.exit(ok ? 0 : 1);
```

Run: `node scripts/verify-pwa-offline.mjs`
Expected: `offline: modulo abriu do cache? true`, exit 0. (Se false: conferir o precache rodou e o SW registrou.)

- [ ] **Step 4: Deletar o throwaway + conferir tree**

Run: `rm scripts/verify-pwa-offline.mjs && git status --short`
Expected: o throwaway sumiu; restam os arquivos novos/modificados da PWA.

- [ ] **Step 5: Commit**

```bash
git add site/manifest.webmanifest site/sw.js site/precache.json site/assets/img/app-icon-*.png site/assets/img/apple-touch-icon.png scripts/gen-pwa-icons.mjs scripts/gen-precache.mjs scripts/inject-pwa-head.mjs test/pwa.test.js site/index.html site/glossario.html site/professor/index.html site/modulos/*.html
git commit -m "feat(pwa): app offline e instalavel (manifest + service worker precache + icones + injecao de head)"
```

---

## Task 8: Deploy no GitHub Pages (Actions) — GATED

**Files:**
- Create: `.github/workflows/pages.yml`

> **Publicação pública.** O usuário autorizou ("eu habilito via gh"). Mesmo assim, **confirmar o comando de habilitar o Pages antes de rodar** (é uma ação externa, irreversível-ish: deixa o site público).

- [ ] **Step 1: Criar o workflow**

Criar `.github/workflows/pages.yml`:

```yaml
name: Deploy PWA to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Regenera o precache (inclui assets novos)
        run: node scripts/gen-precache.mjs
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v4
```

(Observação: `gen-pwa-icons.mjs` NÃO roda no CI — usa Playwright; os PNGs já estão commitados. `gen-precache.mjs` usa só built-ins → roda sem `npm install`.)

- [ ] **Step 2: Commit do workflow**

```bash
git add .github/workflows/pages.yml
git commit -m "ci(pages): workflow de deploy da PWA (publica site/ via Actions)"
```

- [ ] **Step 3: Habilitar o Pages no modo Actions (CONFIRMAR ANTES)**

Antes de rodar, mostrar ao usuário o comando e confirmar. Comando:

```bash
gh api -X POST repos/hhthunderbird/ShaderWorkshop/pages -f build_type=workflow
```
(Se já existir, usar `gh api -X PUT repos/hhthunderbird/ShaderWorkshop/pages -f build_type=workflow`.)

Se o `gh` não tiver permissão/escopo, instruir o usuário a habilitar manualmente: **repo → Settings → Pages → Build and deployment → Source: GitHub Actions.**

- [ ] **Step 4: Push e acompanhar o deploy**

```bash
git push
```
Expected: o workflow roda; após sucesso, o site fica em `https://hhthunderbird.github.io/ShaderWorkshop/`. Conferir: `gh run list --workflow=pages.yml` e abrir a URL; testar instalar/offline no celular.

- [ ] **Step 5: Fechamento**

PWA Fase 0 entregue: app instalável + offline + publicado. Próximas fases (TWA/loja, sala conectada local, redesign+temas) seguem o fluxo próprio. `main` em dia.

---

## Self-Review (cobertura do spec)

- **§3.1 manifest** → Task 2. **§3.2 ícones (SVG + PNGs via Playwright, maskable com safe zone)** → Task 3. **§3.3 sw.js (precache-all, versionado, cache-first, fallback navigate)** → Task 5. **§3.4 gen-precache (built-ins, exclui sw/precache)** → Task 4. **§3.5 injeção depth-aware idempotente** → Task 6. ✓
- **§2 subpath relativo/depth-aware** → manifest `start_url:'.'` (T2); injeção prefixo `''`/`'../'` (T6); SW registra relativo (T6). ✓
- **§4 deploy Actions publicando site/ + habilitar Pages (gated)** → Task 8 (workflow + `gh api` confirmado + push). ✓
- **§5 testes (manifest/sw/precache/injeção) + Playwright offline + smoke 17** → Task 1 (node) + Task 7 (offline + smoke). ✓
- **§7 riscos (subpath, versão de cache v1, maskable safe zone, tamanho precache logado, deploy subpasta via Actions, .md offline)** → tratados em T3/T4/T5/T6/T8. ✓
- **Consistência:** nomes `app-icon-192/512/maskable.png`, `apple-touch-icon.png`, `precache.json`, `sw.js`, marcador `<!-- pwa-head -->` batem entre manifest, scripts e teste. ✓
- **Contagem de testes:** 148 → 152 (T1: +4). ✓
- **Gate externo:** Task 8 Step 3 confirma antes de publicar. ✓
