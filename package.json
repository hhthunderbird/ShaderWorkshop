{
  "name": "shaderworkshop-curso",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs",
    "serve": "python -m http.server 8000 --directory site"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
